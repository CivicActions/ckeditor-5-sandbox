{#
/**
 * @file
 * Default theme implementation to display a taxonomy term
 * in the tooltip view mode.
 */
#}
{{ attach_library('olivero_subtheme/react-components.tooltip') }}
{#
  For the tooltip view mode, output the field_breed_summary
  field value only
#}
{{ term.field_breed_summary.value|trim }}
